#include <stdio.h>
int main(void)
{
int length, width;
printf("Enter the length : ");
scanf("%d", &length);
printf("Enter the width : ");
scanf("%d", &width);
/*Arithmetic operation can be done in the argument */
printf("Area is : %d", length*width);
return 0;
}
