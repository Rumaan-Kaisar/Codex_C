#include<stdio.h>
int main(void)
{
    char ch;
    printf(" enter the character :");
    ch=getchar(); /*Read a single character from keyboard like scanf() */
    printf("\nYou have entered the character : %c", ch);
    return 0;
}
